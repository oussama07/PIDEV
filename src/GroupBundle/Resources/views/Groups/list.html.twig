{% extends 'baseClient.html.twig' %}
{% block header %}

{% endblock %}

{% block container %}
<style>
    #btn1{
        border:none;
        padding:5px 0 5px 0;
        border-radius:20px;
        background:rgb(2, 180, 204);
        font:bold 20px Arial;
        color:#fff;
        margin:10px 0px;
    }
    .bouton13{
        border-radius:20px;
        display: block;
        text-align: center;
        color: white;
    }
</style>
    <div align="center">
<H2>Liste Groups </H2>
<table border="0px" >

    <tr><th>Nom</th> <th>Description</th></tr>
    {%for c in listG %}
        {% set A = 0 %}
        <tr>

            <td>{{ c.nom()}}</td>
            <td>{{ c.description }}</td>
            {% for t in listExi  %}
                {% if c.idF == id and A == 0 %}
                <td id="btn1" ><a href="#"  class="bouton13">Votre group</a></td>
                    {% set A = 1 %}

                {%elseif t.id== c.id and A == 0 %}
                    <td id="btn1"><a href="{{ path('quitter',{'id':c.id}) }}"   class="bouton13">quitter</a></td>
                    {% set A = 1 %}
                {% endif %}
            {% endfor %}

            {%if A == 0 %}
                <td id="btn1" ><a href="{{ path('joingroup',{'idGroup':c.id}) }}"  class="bouton13">rejoindre</a></td>
            {% endif %}
        </tr>

    {% endfor %}
</table>
    </div>
{% endblock %}